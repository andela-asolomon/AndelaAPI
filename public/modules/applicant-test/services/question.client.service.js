'use strict';

//Questions service used for communication with the articles REST endpoints
angular.module('applicant-test').factory('Articles', ['$resource',
    function($resource) {
        return $resource('')
    }])